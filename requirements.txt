# ── 核心 ─────────────────────────────────────────────
# streamlit 鎖定雲端實跑版本，避免新版改動 @st.fragment 等行為（見 CLAUDE.md 陷阱）
streamlit==1.37.1
# pandas-ta 只有 pre-release 版本（0.4.x）；pin 確切版本同時讓 uv 願意解析其 pre-release。
# ⚠️ 重要教訓：pandas-ta 0.4.x 依賴 pandas>=2.3.2，且早已無舊版 numpy<2 的 NaN import 問題。
#   切勿替 numpy/pandas 加 <2 / <2.3 上限——會與 pandas-ta 硬衝突，雲端直接 No solution。
pandas-ta==0.4.71b0
numpy>=1.26
pandas>=2.3.2
plotly>=5.0
# ── 資料源 ───────────────────────────────────────────
yfinance>=0.2.52
ccxt>=4.5.10
requests>=2.31.0
urllib3>=2.0
# [Task #2] 非同步 HTTP 客戶端，用於並行抓取資金費率分頁
httpx>=0.27
# [Task #8] 環境變數管理，從 .env 讀取 API Key
python-dotenv>=1.0
# [Task #9] LINE Bot 推播通知
line-bot-sdk>=3.22.0
# [Task #10] 單元測試框架
pytest>=7.0
